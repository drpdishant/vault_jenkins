# project_name: curd_demo
project_root: /srv/www
#project_type: laravel
nginx_config_root: /home/addweb/addweb-docker-stack/nginx_conf
project_repo: "{{ lookup('env','GIT_URL') }}"
git_protocol: http
magicdns: true
php_version: 7.2
db_user: "{{ project_type }}"
db_pass: "{{ project_type }}"
db_name: "{{ project_type }}_{{ project_name.stdout | trim }}"
db_host: mariadb
